import styled from 'styled-components';

export const StyledBackButton = styled.button`
    border: 1px solid var(--clr-admin-gold);
    border-radius: 0.4em;
    width: fit-content;
    background-color: white;

    a {
        vertical-align: middle;

        svg {
            vertical-align: middle;
        }
    }
    
`